spring:
  application:
    name: main_incident_dashboard
  profiles:
    active: ${SPRING_PROFILE_ACTIVE:qa}

server:
  port: ${SERVER_PORT:8080}

firebase:
  config-path: classpath:firebase-service-account.json